% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/use_albersdown.R
\name{use_albersdown}
\alias{use_albersdown}
\title{One-shot setup for existing packages}
\usage{
use_albersdown(
  family = "red",
  apply_to = c("all", "new"),
  dry_run = FALSE,
  fallback_extra = c("auto", "always", "never")
)
}
\arguments{
\item{family}{one of: "red","lapis","ochre","teal","green","violet"}

\item{apply_to}{"all" to patch every \emph{.Rmd/}.qmd in vignettes/, or "new" to only add the template and assets}

\item{dry_run}{if TRUE, show changes without writing}

\item{fallback_extra}{Controls copying site-wide fallbacks into \verb{pkgdown/}:
\itemize{
\item "auto": copy \code{pkgdown/extra.css} and \code{pkgdown/extra.js} only when the consuming
package does not use \code{template: { package: albersdown }} in \verb{_pkgdown.yml}.
\item "always": always copy to \verb{pkgdown/} (useful as a safety net or for custom setups).
\item "never": never copy site-wide fallbacks.
}}
}
\description{
Turn-key retrofit to adopt the albersdown theme in an existing package.
Copies local assets for CRAN-safe vignettes, ensures pkgdown template,
optionally patches all vignettes, writes a README note, and prints a doctor report.
}
